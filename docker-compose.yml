version: '3.8'

services:
  hoodini:
    build:
      context: .
      dockerfile: Dockerfile
    image: hoodini:latest
    volumes:
      # Mount current directory as working directory
      - .:/work
    environment:
      - PYTHONUNBUFFERED=1
